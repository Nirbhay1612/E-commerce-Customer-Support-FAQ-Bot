# ── Core Web Framework ──────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.9          # for file uploads if needed
pydantic>=2.9.0
pydantic-settings>=2.5.0

# ── LLM & Agent Framework ───────────────────────────────────────────
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0          # OpenAI (or swap with others below)
langchain-groq>=0.2.0            # optional – free/fast alternative
langgraph>=0.2.0                 # for multi-agent / stateful workflows
langchain-text-splitters>=0.3.0

# ── RAG / Vector Store ──────────────────────────────────────────────
chromadb>=0.5.0                  # popular local vector DB
# pinecone-client>=5.0.0         # uncomment if using Pinecone
# faiss-cpu>=1.8.0               # alternative lightweight option
sentence-transformers>=3.0.0     # local embeddings (all-MiniLM-L6-v2 etc.)
langchain-huggingface>=0.1.0
langchain-chroma>=0.2.0

# ── Database & ORM ──────────────────────────────────────────────────
sqlalchemy>=2.0.0
aiosqlite>=0.20.0                # async SQLite
# asyncpg>=0.29.0                # if using PostgreSQL
# pymysql>=1.1.0                 # if using MySQL

# ── Utilities ───────────────────────────────────────────────────────
python-dotenv>=1.0.0
pandas>=2.2.0
requests>=2.32.0
httpx>=0.27.0
numpy>=1.26.0
tiktoken>=0.7.0                  # token counting for OpenAI
pypdf>=5.0.0                     # if loading product PDFs / manuals

# ── Frontend (optional) ─────────────────────────────────────────────
streamlit>=1.40.0                # quick chatbot UI

# ── Observability (optional) ────────────────────────────────────────
langsmith>=0.1.0                 # LangSmith tracing

# ── Production extras (optional) ────────────────────────────────────
gunicorn>=23.0.0                 # if deploying with Flask/Gunicorn
redis>=5.0.0                     # for caching / session / Celery